
#-----------------------------------------------------------------------------#

install-package

Used to install a previously built package.

Workflow targets:

	select-action-params (PACKAGE, INSTALL, REPO PATH, INSTALLROOT)
	select-repo-packages
	package-install

